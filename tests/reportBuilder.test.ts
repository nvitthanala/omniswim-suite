import { describe, it, expect } from 'vitest';import { buildMeetReportHtml } from '../packages/core/src/lib/reportBuilder';import type { Workspace } from '../packages/core/src/types';describe('reportBuilder', () => {  it('builds HTML report with workspace name', () => {    const ws: Workspace = {      id: 'w1',      name: 'Conference Finals',      createdAt: Date.now(),      menResults: [],      womenResults: [],      recruits: [],      deletedSwimmers: [],    };    const html = buildMeetReportHtml(ws);    expect(html).toContain('Conference Finals');    expect(html).toContain('<!DOCTYPE html>');  });});