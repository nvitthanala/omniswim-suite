import { useCallback, useEffect, useRef, useState, type ReactNode } from 'react';import { X } from 'lucide-react';export type FloatingWindowState = {  x: number;  y: number;  w: number;  h: number;};type Props = {  title: string;  open: boolean;  onClose: () => void;  state: FloatingWindowState;  onStateChange: (next: FloatingWindowState) => void;  minWidth?: number;  minHeight?: number;  headerActions?: ReactNode;  children: ReactNode;};type DragMode = 'move' | 'resize' | null;export function FloatingWindow({  title,  open,  onClose,  state,  onStateChange,  minWidth = 280,  minHeight = 200,  headerActions,  children,}: Props) {  const [dragMode, setDragMode] = useState<DragMode>(null);  const dragStart = useRef({ x: 0, y: 0, winX: 0, winY: 0, winW: 0, winH: 0 });  const clampPosition = useCallback(    (x: number, y: number, w: number, h: number) => {      const maxX = Math.max(0, window.innerWidth - w);      const maxY = Math.max(0, window.innerHeight - h);      return {        x: Math.min(Math.max(0, x), maxX),        y: Math.min(Math.max(0, y), maxY),      };    },    []  );  const handlePointerDownMove = (e: React.PointerEvent) => {    if (e.button !== 0) return;    e.preventDefault();    setDragMode('move');    dragStart.current = {      x: e.clientX,      y: e.clientY,      winX: state.x,      winY: state.y,      winW: state.w,      winH: state.h,    };    (e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);  };  const handlePointerDownResize = (e: React.PointerEvent) => {    if (e.button !== 0) return;    e.preventDefault();    e.stopPropagation();    setDragMode('resize');    dragStart.current = {      x: e.clientX,      y: e.clientY,      winX: state.x,      winY: state.y,      winW: state.w,      winH: state.h,    };    (e.currentTarget as HTMLElement).setPointerCapture(e.pointerId);  };  useEffect(() => {    if (!dragMode) return;    const onMove = (e: PointerEvent) => {      const dx = e.clientX - dragStart.current.x;      const dy = e.clientY - dragStart.current.y;      if (dragMode === 'move') {        const pos = clampPosition(          dragStart.current.winX + dx,          dragStart.current.winY + dy,          state.w,          state.h        );        onStateChange({ ...state, ...pos });      } else {        const w = Math.max(minWidth, dragStart.current.winW + dx);        const h = Math.max(minHeight, dragStart.current.winH + dy);        const maxW = window.innerWidth - state.x;        const maxH = window.innerHeight - state.y;        onStateChange({          ...state,          w: Math.min(w, maxW),          h: Math.min(h, maxH),        });      }    };    const onUp = () => setDragMode(null);    window.addEventListener('pointermove', onMove);    window.addEventListener('pointerup', onUp);    return () => {      window.removeEventListener('pointermove', onMove);      window.removeEventListener('pointerup', onUp);    };  }, [dragMode, state, onStateChange, clampPosition, minWidth, minHeight]);  if (!open) return null;  return (    <>      {dragMode ? (        <div          className="floating-window-overlay"          style={{ cursor: dragMode === 'move' ? 'grabbing' : 'nwse-resize' }}          aria-hidden        />      ) : null}      <div        className="floating-window"        style={{ left: state.x, top: state.y, width: state.w, height: state.h }}        role="dialog"        aria-label={title}      >        <div className="floating-window-header" onPointerDown={handlePointerDownMove}>          <span className="floating-window-title">{title}</span>          {headerActions}          <button            type="button"            onClick={onClose}            onPointerDown={e => e.stopPropagation()}            className="btn-ghost p-1.5"            aria-label="Close"          >            <X size={14} />          </button>        </div>        <div className="floating-window-body">{children}</div>        <div          className="floating-window-resize"          onPointerDown={handlePointerDownResize}          aria-hidden        />      </div>    </>  );}