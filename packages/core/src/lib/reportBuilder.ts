import type { Workspace } from '../types';import { buildSeasonTrends } from './seasonAnalytics';function esc(s: string): string {  return s.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;');}export function buildMeetReportHtml(workspace: Workspace): string {  const trends = buildSeasonTrends([workspace]);  const menCount = workspace.menResults?.length ?? 0;  const womenCount = workspace.womenResults?.length ?? 0;  const generated = new Date().toLocaleString();  const swimmerRows = trends.swimmerTrends    .slice(0, 50)    .map(      t => `<tr><td>${esc(t.name)}</td><td>${esc(t.event)}</td><td class="mono">${esc(t.bestTime)}</td><td>${t.meetCount}</td></tr>`    )    .join('');  return `<!DOCTYPE html><html lang="en"><head>  <meta charset="utf-8" />  <title>${esc(workspace.name)} ΓÇö Meet Report</title>  <style>    body { font-family: system-ui, sans-serif; max-width: 900px; margin: 2rem auto; padding: 0 1rem; color: #111; }    h1 { font-size: 1.5rem; margin-bottom: 0.25rem; }    .meta { color: #666; font-size: 0.875rem; margin-bottom: 1.5rem; }    table { width: 100%; border-collapse: collapse; font-size: 0.875rem; }    th, td { border: 1px solid #ddd; padding: 0.5rem 0.75rem; text-align: left; }    th { background: #f4f4f5; }    .mono { font-family: ui-monospace, monospace; }    @media print { body { margin: 0; } }  </style></head><body>  <h1>${esc(workspace.name)}</h1>  <p class="meta">Omni Swim Suite ┬╖ Generated ${esc(generated)} ┬╖ ${menCount} men&apos;s + ${womenCount} women&apos;s results</p>  ${workspace.conference ? `<p><strong>Conference:</strong> ${esc(workspace.conference)}</p>` : ''}  ${workspace.loadedMeet?.meetLabel ? `<p><strong>Meet:</strong> ${esc(workspace.loadedMeet.meetLabel)}</p>` : ''}  <h2>Top swimmer trends</h2>  <table>    <thead><tr><th>Swimmer</th><th>Event</th><th>Best</th><th>Data points</th></tr></thead>    <tbody>${swimmerRows || '<tr><td colspan="4">No trend data</td></tr>'}</tbody>  </table>  <p class="meta" style="margin-top:2rem">Print this page (Ctrl+P) to save as PDF.</p></body></html>`;}