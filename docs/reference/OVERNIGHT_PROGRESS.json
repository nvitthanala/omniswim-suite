{
  "purpose": "Running log for the overnight 2026-09-21/22 session. The user is asleep and asked that work continue, that progress be logged so a rate limit loses nothing, and that the whole thing be finished inside the remaining 7-day quota. An hourly cron job (created in-session) re-enters this file and takes the next item.",
  "readThisFirstOnResume": "This file, then docs/reference/CRAWL_INVESTIGATION_STATE.json. Do not re-derive anything either one already records. Take the top unfinished item from `nextUp`, finish it completely, append to `timeline`, and update this file before moving on.",
  "hardRules": [
    "NEVER fetch swimcloud.com. Everything comes from the stored capture in data/swimcloud-captures/, the code, or committed fixtures. Touching the live site is a deliberate human-present action.",
    "Never fabricate competition data. Absent is not empty. Parsers fail loudly.",
    "Nothing hardcoded that must vary by division, team, scoring format or meet format.",
    "A guard is untrusted until it has been watched to fail. Mutation-test every new test. An inert mutation is not evidence -- say so and strengthen the test.",
    "All three gates clean before commit: npm run lint, npx vitest run, node scripts/run-tests.mjs.",
    "No subagents. Quota is the binding constraint, and a cold-start agent re-derives context this session already holds."
  ],
  "quotaAtStart": {
    "at": "2026-09-22T04:23Z",
    "userReported": "31% of the 7-day window left",
    "localTelemetry": "trailing 7d: ~1.04B tokens across opus+sonnet, almost all cache-read",
    "implication": "Be economical. Finish one item completely rather than starting several. Prefer reading a grep result over re-reading a large file."
  },
  "doneThisSession": [
    {
      "commit": "02f22903",
      "what": "Relay legs (queue item A) and dash-points (item D). Legs come from the hidden js-hidden-list table behind the page's own 'Show names' toggle -- 4 per relay with names, swimmer ids and splits."
    },
    {
      "commit": "63be4500",
      "what": "Event enumeration (item B). readMeetEventIndex reads the 57-event index every event page prints; the event pass expands from it in rounds. Swims-derived enumeration found 51 and missed four diving events and two mixed relays."
    },
    {
      "commit": "8cf719f2",
      "what": "Event-first import plus class year (item C). swimCloudEventResultsToSwimmerResults converts event pages into rows directly. Recovered 71 relay rows that were all being dropped, and joined class year by swimmer id for 1081 of 1089 individual rows."
    },
    {
      "commit": "5880fc46",
      "what": "Swimmer-times endpoint discovery. Read from Resource Timing on a page the coach opens, never hardcoded. Nothing is fetched from it yet -- its method and response shape are unobserved."
    },
    {
      "commit": "b2e1dd9b",
      "what": "Recorded all of the above in CRAWL_INVESTIGATION_STATE.json. Pushed to the branch and fast-forwarded main."
    },
    {
      "commit": "efda8f76",
      "what": "n1 found a real defect by reading the loop, not the tests: a re-crawl of a complete capture fetches nothing, so no page index is ever read, so the four diving events would stay missing forever. storedEventRefs now reads the index off stored meetEvent pages too. Two mutations proven lethal (one was inert on the first attempt and was redone)."
    },
    {
      "commit": "bf58e166",
      "what": "n2: the pre-crawl estimate counted swims pages the planner no longer plans and called the event count unknown when the meet had published it. A 4-team 57-event meet now quotes 57 requests, not 8. A test asserts the estimate and planScopedMeetCrawl agree rather than merely looking similar."
    },
    {
      "commit": "93cc5161",
      "what": "n3: USER_GUIDE.md section 4 rewritten. It described a crawl that no longer happens. Now covers event-by-event crawling, why diving appears for the first time, the class-year column and its four unknown reasons, and the endpoint-report button."
    },
    {
      "commit": "29c7a227",
      "what": "n6: a Mixed event (403, 503) reported \"Gender could not be determined\", which reads like a parse failure on a page that parsed fine. The parse result now keeps the printed gender word verbatim and the converter has a distinct, expected-severity skip reason. A control test pins that an unrecognised word still reports unknown-gender."
    },
    {
      "commit": "e877acce",
      "what": "n5 part 1: getCutlinesForSwim/compareTimeToCutline defaulted division to D1, and isACut/isBCut took it as optional and passed it through -- so isACut(sec,gender,event) measured any swim against the D1 table. CLAUDE.md rule 5 forbids exactly that. No caller omitted it, so nothing shipped wrong; the fix makes tsc enforce the rule. Stated honestly in the test that restoring the default fails nothing today."
    },
    {
      "commit": "ab60c08e",
      "what": "n5 part 2: deleted NEON_COLORS and TEAM_COLORS_MAP, dead constants naming four specific schools. Readiness f9 marked resolved; its recorded consequence was already stale."
    },
    {
      "commit": "ba5af5ad",
      "what": "n4: 429 backoff and retry on both fetch paths. Honours Retry-After, else 3s/6s/12s, gives up past 60s or three tries, reports the wait on the panel. Nothing speeds up -- a Retry-After under the 3s floor is raised to it. Writing the tests found a real bug: Date.parse accepts \"1.5\" and returned a 2001 timestamp, collapsing the wait to zero."
    },
    {
      "commit": "2bf5f7fd",
      "what": "800 and 1500 Freestyle were convertible but not plannable, so a long-course meet could not be planned. Added, with the missing invariant: every event CONVERSION_FACTORS knows must be in the catalogue. Readiness f8 was half stale."
    },
    {
      "commit": "b97c8594",
      "what": "n7 first finding: the Rule 7 relationship that a championship relay table is exactly twice its individual table was true in all six field-size presets but asserted nowhere. A single mistyped relay value would have passed every test while mis-scoring a double-weighted event. Now asserted as a relationship between the tables, with the dual-meet exception (11-4-2 is not twice 9-4-3-2-1) asserted in the same file so nobody tidies it away."
    },
    {
      "commit": "(vault, not git)",
      "what": "Obsidian vault updated per the refreshed CLAUDE.md: new Sessions note, 02-Invariants gained a section 3 on the event-first invariants, 05-Timeline gained Era 9, and the stale branch-divergence claim in 04 and 08 was corrected -- main now contains both previously-diverged branch tips, verified with git merge-base rather than assumed."
    },
    {
      "commit": "0bf3d192",
      "what": "n8: navigability. Checked every internal link target against the shell routes -- all seven resolve, nothing dead, Metrics reachable from the applet switcher marked experimental. Added the guard, because the routes live in JSX and the targets live across three packages with nothing comparing them: deleting a route leaves every link to it apparently working, since the catch-all redirects to \"/\"."
    },
    {
      "commit": "68f311a6",
      "what": "Found by continuing the n7 hunt after the queue emptied, and the most consequential defect of the night: nothing on the SwimCloud path ever set isTimeTrial, so meet 356467s 13 post-programme events (14 swims in the capture) imported as ordinary scoring swims AND counted against every swimmers entry limit -- breaking the users own explicit ruling on the new primary import path. Boundary read from ScoringSettings.scoredEventNumberMax, never a literal. Programme number read from the badge the event index prints, not the URL."
    },
    {
      "commit": "4955ec9b",
      "what": "Diving events parsed for the first time. The pages had never been fetched, and when they were, they failed outright: a diving table has no Time column. 47 rows, 360 meet points, cross-checked row by row against data/meets.json. Also settles the long-open question -- a diving Score IS meet points."
    },
    {
      "commit": "d4bd313b",
      "what": "Coach's ruling on the two non-ordinary SwimCloud row kinds. A relay leadoff is now imported as the individual swim it is (starts from the blocks, finishes to the hand); the real capture went from 8 imported swims to 9. An extracted time is now tagged and routed to extractedByEvent, where it fills a relay leg the swimmer has no standalone time for, and is kept out of bestByEvent so it is never ranked, cut-tagged or entered. Four mutations run; two were inert on the first attempt -- one patched the wrong converter, the other could not be distinguished by the real fixture -- and both were redone or covered by a synthetic case."
    }
  ],
  "nextUp": [
    {
      "id": "n9",
      "title": "Keep the n7 hunt going on paths not yet read",
      "why": "Six real defects tonight were found by reading code in an already-green suite, the last one after the queue was empty. The arbitrage, rosterOptimizer and relay-construction paths have not been read this way.",
      "how": "One path at a time. Write the failing test before the fix. Mutation-test every guard and treat an inert mutation as a finding about the test, not a pass.",
      "status": "next"
    }
  ],
  "blockedNeedsTheUser": [
    "Which discovery page carries the event index. Deciding it needs one live fetch, which this session may not make. Both branches are implemented and correct; the panel says which one ran.",
    "The swimmer-times endpoint's method and response shape. The user clicks 'Copy times endpoint for Omniswim' on one swimmer page and pastes the report. No season-best pass can exist before that."
  ],
  "timeline": [
    {
      "at": "2026-09-22T04:30Z",
      "what": "Opened this file. Hourly cron job 146cdc7e created to re-enter the work if a rate limit interrupts it. Cron jobs are session-only and auto-expire after 7 days -- if Claude exits, the schedule is gone and the user must restart it."
    },
    {
      "at": "2026-09-22T05:05Z",
      "what": "n1 and n2 done, pushed to branch and main. 1030 vitest, 84 script tests, lint clean. Also noted during review: a Mixed-gender event (403, 503) parses as gender unknown, so every row on it skips with the reason label \"Gender could not be determined\" -- misleading, since the gender IS determined and is Mixed. Recorded as n6."
    },
    {
      "at": "2026-09-22T06:05Z",
      "what": "n3, n5, n6 done. Hardcode sweep results: NO hardcoded SwimCloud team or meet ids in production code. Rule 7 point tables and SCORING_POINTS are legitimate domain data behind the preset layer. Two real findings: the D1 division default (fixed, e877acce) and dead hardcoded constants NEON_COLORS/TEAM_COLORS_MAP naming four specific schools (deleted; readiness finding f9 marked resolved, its recorded consequence was already stale)."
    },
    {
      "at": "2026-09-22T06:40Z",
      "what": "Queue n1-n6 cleared plus f8. 1053 vitest, 84 script tests, lint clean, all pushed to branch and main. Every new guard mutation-tested; three mutations were inert on the first attempt and were redone rather than counted."
    },
    {
      "at": "2026-09-22T07:20Z",
      "what": "Resumed after the 5h limit reset. Finished the relay-doubling test that was mid-mutation when the limit hit: the inert mutation was itself a finding (double-dual-tri-quad was covered by no test at all) and that format is now covered. Four mutations proven lethal. 1066 vitest, 84 script tests, lint clean. Then honoured the Obsidian vault instruction that arrived with the refreshed CLAUDE.md."
    },
    {
      "at": "2026-09-22T07:45Z",
      "what": "n8 done and the queue is empty. Three mutations proven lethal on the navigation guard, one of which reproduced a blind spot my own manual sweep had had (object-literal to: entries). 1070 vitest, 84 script tests, lint clean, pushed to branch and main."
    },
    {
      "at": "2026-09-22T08:15Z",
      "what": "Queue was empty so kept hunting rather than idling, and found the isTimeTrial gap. Three mutations run; the third was inert because the printed number and the URL ref are identical on all 51 captured pages, so a synthetic case where they diverge was added to separate them. 1077 vitest, 84 script tests, lint clean."
    },
    {
      "at": "2026-09-22T14:45Z",
      "what": "User ran the full-field crawl and pasted a rendered swimmer-times page. Verified the capture (57 event pages), answered and fixed diving, and found the Extracted-vs-Leadoff asymmetry. Also noted the local workspace went from 550 menResults at 14:05 to 0 from 14:25 -- recoverable from data/backups/meets-startup-2026-09-22T14-05-01-773Z.json -- which is why two e2e tests now fail."
    },
    {
      "at": "2026-09-22T15:30Z",
      "what": "Implemented the extracted/leadoff ruling. 16 existing assertions across 5 files encoded the old behaviour and were updated with their reasoning, not just their numbers. 1097 vitest, lint clean. The 2 playwright failures are unchanged and environmental: the local workspace DB holds one empty workspace, so the seed test and the chart test have nothing to work with."
    }
  ],
  "notesForTheUser": [
    "divisionForTeam and registerTeamsFromList are exported, deprecated, and answer D1 for an unknown team. Nothing in the repo calls them, but scripts/test_cutline_tags.mjs deliberately pins the legacy behaviour, so they were left alone. Deleting exported API is your call -- say the word and they go.",
    "Readiness finding f8 is still open and is a real limit on \"usable for all formats\": INDIVIDUAL_EVENTS does not list 400 Freestyle, though the conversion table already knows it. A coach planning a long-course meet cannot pick an event the program actually contests.",
    "The UI redesign initiative (docs/reference/UI_REDESIGN_STATE.json, 19 turns) has three items the 2026-09-14 session DEFERRED on purpose rather than rushed: optimizer rejected-candidate surfacing (a real rosterOptimizer candidate-selection change, executor-tier), optimizer undo, and the converted-time cutline half of the pre-submit checklist. I did not start any of them overnight -- each is a design decision, not a fix, and starting one on limited quota while you were asleep was the wrong call. They are yours to sequence."
  ],
  "queueState": "EMPTY as of 2026-09-22T08:15Z, but the n7-style hunt keeps paying: it found the isTimeTrial gap after the queue was already clear. If resuming with quota to spare, keep reading a path at a time rather than inventing queue items -- the pattern that works is looking for a default standing in for a missing fact, two tables that must agree with nothing asserting it, and an absent value treated as zero."
}
