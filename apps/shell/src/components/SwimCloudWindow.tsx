import { useEffect, useState } from 'react';import { ExternalLink } from 'lucide-react';import { FloatingWindow, type FloatingWindowState, useSwimCloudWindow } from '@omniswim/ui';const STORAGE_KEY = 'omni-swimcloud-window';const DEFAULT_STATE: FloatingWindowState & { open: boolean } = {  open: false,  x: 80,  y: 80,  w: 480,  h: 560,};function loadState(): FloatingWindowState & { open: boolean } {  if (typeof window === 'undefined') return DEFAULT_STATE;  try {    const raw = window.localStorage.getItem(STORAGE_KEY);    if (!raw) return DEFAULT_STATE;    const parsed = JSON.parse(raw) as Partial<FloatingWindowState & { open: boolean }>;    return {      open: Boolean(parsed.open),      x: typeof parsed.x === 'number' ? parsed.x : DEFAULT_STATE.x,      y: typeof parsed.y === 'number' ? parsed.y : DEFAULT_STATE.y,      w: typeof parsed.w === 'number' ? parsed.w : DEFAULT_STATE.w,      h: typeof parsed.h === 'number' ? parsed.h : DEFAULT_STATE.h,    };  } catch {    return DEFAULT_STATE;  }}export default function SwimCloudWindow() {  const { open, closeWindow } = useSwimCloudWindow();  const [persisted, setPersisted] = useState(loadState);  useEffect(() => {    setPersisted(prev => ({ ...prev, open }));  }, [open]);  useEffect(() => {    window.localStorage.setItem(STORAGE_KEY, JSON.stringify({ ...persisted, open }));  }, [persisted, open]);  const handleStateChange = (next: FloatingWindowState) => {    setPersisted(prev => ({ ...prev, ...next }));  };  return (    <FloatingWindow      title="SwimCloud"      open={open}      onClose={closeWindow}      state={{ x: persisted.x, y: persisted.y, w: persisted.w, h: persisted.h }}      onStateChange={handleStateChange}      minWidth={320}      minHeight={280}      headerActions={        <a          href="https://www.swimcloud.com/"          target="_blank"          rel="noreferrer"          className="btn-ghost p-1.5 text-[var(--text-accent)]"          title="Open in new tab"          onPointerDown={e => e.stopPropagation()}        >          <ExternalLink size={14} />        </a>      }    >      <iframe        title="SwimCloud reference"        src="https://www.swimcloud.com/"        className="w-full h-full border-0 bg-white"        sandbox="allow-scripts allow-same-origin allow-popups allow-forms"      />    </FloatingWindow>  );}