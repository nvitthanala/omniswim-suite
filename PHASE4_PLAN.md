# Omni Swim Suite ΓÇö Phase 4 Proposal> Living proposal. Pick one or more epics below to start Phase 4. Do not begin implementation until scope is confirmed.Last updated: 2026-06-19.---## ContextPhase 3 delivered multi-user auth, SQLite/PostgreSQL persistence, season analytics, metrics compare, reporting/share links, Tauri desktop packaging, and CI. A follow-up chart/theming pass fixed Metrics velocity charts and Matrix line-chart edge cases (single-point visibility, tooltip index normalization).---## Candidate epics (pick scope)| id | epic | summary | effort ||----|------|---------|--------|| p4-biomechanics | Real video biomechanics | Replace simulated `calculateMetricsLocal` with opt-in local frame/pose analysis; keep `/api/analyze-video` 501 unless user enables cloud AI | Large || p4-analytics | Season dashboard v2 | Per-swimmer progression charts, cut-line trends, team-vs-team season comparisons on `/analytics`. **Note:** meet-level prelims over/under is implemented in Matrix (2026-06-28); season-wide prelims trends remain a future analytics item. | Medium || p4-collab | Live collaboration | Build on auth + sync polling: presence indicators, conflict resolution, shared cursors on workspace edits | Large || p4-reporting | Advanced reporting | PDF exports, branded meet reports, optional scheduled email (postgres deployments) | Medium || p4-recruiting | Recruiting & projection | Class-year attrition, future-cut forecasting, roster projection wizard in Manager | Medium || p4-desktop | Tauri polish | Auto-update, offline-first SQLite sync, native file pickers for PDF/video | Medium |---## Recommended starting point (if undecided)**p4-analytics** ΓÇö highest value for coaches already using Matrix + Manager data; builds on `seasonAnalytics.ts` and `/analytics` without new infrastructure.Secondary quick win: **p4-reporting** PDF layer on existing `reportBuilder.ts`.---## Architecture notes (unchanged constraints)- No external/cloud AI by default.- `SuiteWorkspaceProvider` and `useWorkspaceScoring` return shapes are frozen.- `packages/core` must NOT import `@omniswim/ui`.---## Suggested Phase 4 todo table (template)| id | description | status ||----|-------------|--------|| p4-analytics-progression | Swimmer event progression line charts on `/analytics` | pending || p4-analytics-cuts | Cut-line achievement trends by team/class | pending || p4-reporting-pdf | HTML report ΓåÆ PDF export (client-side print or server puppeteer) | pending || p4-recruiting-view | Manager recruiting projection tab | pending |---## Verification (when Phase 4 starts)```powershellcd "C:\Users\nihar\Documents\GitHub\omniswim-suite\omniswim-suite"npm installnpm testnpm run -w @omniswim/shell build```Manual smoke per epic (document in epic PR).---## How to confirm scopeReply with epic id(s) from the table above (e.g. `p4-analytics` + `p4-reporting`), or describe a custom priority order.